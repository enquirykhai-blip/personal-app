rules_version = '2';

// A single document shared by everyone who opens the app. Any signed-in user
// (anonymous counts) may read and write it, so all users share the same data.
service cloud.firestore {
  match /databases/{database}/documents {
    match /shared/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}